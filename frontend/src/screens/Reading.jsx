export default function Reading({ nav }) {
    return (
      <div style={{padding: '20px'}}>
        <h2>🃏 Reading</h2>
        <button onClick={() => nav('home')}>უკან</button>
      </div>
    )
  }