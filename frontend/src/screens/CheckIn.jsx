export default function CheckIn({ nav }) {
    return (
      <div style={{padding: '20px'}}>
        <h2>📓 CheckIn</h2>
        <button onClick={() => nav('home')}>უკან</button>
      </div>
    )
  }